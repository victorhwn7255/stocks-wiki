# Bulk raw sources — re-fetchable via scripts/fetch_filings.py
raw/filings/
raw/transcripts/
raw/news/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# macOS metadata
.DS_Store

# Editor / IDE noise
.vscode/
.idea/
*.swp
*.swo

# Obsidian (per-vault config; workspace.json rewrites constantly)
.obsidian/
.obsidian.bak/

prompts/

automation/digests/
automation/prompts/
# Research-loop transient working dirs (state + findings per run)
automation/research-runs/

# Generated website (build artifact — regenerate with `python web/build.py`)
web/dist/
raw/HTML/

# Proprietary third-party research PDFs (LSEG/Fundstrat/JPM — redistribution prohibited).
# The extracted data (forward-earnings-raw-data.csv) IS tracked; the source PDFs are not.
raw/data/forward-earnings/LSEG_reports/
raw/reports/

# Staged source captures (Vic-staged Twitter/X threads + third-party reports) — kept
# local for the intel skills to read; the analysis lands in raw/notes/*-intel/, which IS tracked.
raw/notes/twitter-notes/
raw/notes/reports/

TODO.md
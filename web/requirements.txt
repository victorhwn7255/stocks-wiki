# Build dependencies for the static site generator (web/build.py).
# Vercel installs these during the build step. Pinned loosely; the generator uses stable APIs.
jinja2>=3.1
markdown>=3.5
pyyaml>=6.0
